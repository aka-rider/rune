Intro paragraph, cursor lands here rather than inside a quote below.

> A single-line blockquote.

> A multi-line blockquote that continues
> onto a second line of quoted text.

Nested blockquotes:

> outer quote line
>
> > inner quote line
> > second inner line
>
> back to outer

A blockquote containing **bold**, *italic*, and `code`:

> quoted text with **bold** and *italic* and `code` inside it

A blockquote with a long line to check wrapping behaviour inside quoted
text when the content runs past the viewport width and must wrap onto a
second visual row while staying inside the quote marker.

> this line inside a blockquote is deliberately long enough that it should wrap onto more than one visual row inside the editor

Trailing paragraph after every blockquote above.
