Intro paragraph, cursor lands here rather than on a list line below.

- first bullet item
- second bullet item
- third bullet item with **bold** inside

Nested bullets:

- outer one
  - inner one
  - inner two
- outer two

Ordered list:

1. first ordered item
2. second ordered item
3. third ordered item

Mixed ordered with nesting:

1. outer step one
   1. inner step a
   2. inner step b
2. outer step two

A long bullet line to check wrap behaviour continues correctly when the
text runs past the viewport width and has to wrap onto a second row.

Trailing paragraph after every list above.
