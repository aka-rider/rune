Intro paragraph, cursor lands here rather than on a task line below.

- [ ] an unchecked task
- [x] a checked task
- [X] a checked task with a capital X

Nested tasks:

- [ ] outer unchecked
  - [ ] inner unchecked
  - [x] inner checked
- [x] outer checked

Tasks mixed with plain bullets:

- [ ] a task item
- a plain bullet, not a task
- [x] another task item

A task with **bold** and `code` inside its label:

- [ ] task with **bold** label
- [x] task with `inline code` label

Trailing paragraph after every task list above.
