[package]
name = "rune-vfs"
version = "0.1.0"
edition.workspace = true

[features]
strict-invariants = []
fault-injection = []

[dependencies]
libc = { workspace = true }
trash = { workspace = true }
rune-core = { path = "../rune-core" }
sha2 = "0.10"

[dev-dependencies]
rune-vfs = { path = ".", features = ["strict-invariants", "fault-injection"] }

[lints]
workspace = true
