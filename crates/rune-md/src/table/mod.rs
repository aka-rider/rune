//! Table rendering (WP2.S5 onward): span-tiling, cell rendering, and Grid/
//! Wrapped/Pivoted layout for GFM tables. Declared now so the crate builds
//! with the module in place; its contents land in a later step.
